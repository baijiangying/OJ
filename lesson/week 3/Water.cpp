#include <cstdio>

int solve ( int, int, int, int );

int main() {
	int n, m, t, d;
	scanf ( "%d%d%d%d", &n, &m, &t, &d );

	printf ( "%d\n", solve ( n, m, t, d) );
	return 0;
}

int solve ( int, int, int, int ) {
	
}